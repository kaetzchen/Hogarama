apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hogarama-commons.fullname" . }}-test-connection"
  labels:
    {{- include "hogarama-commons.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hogarama-commons.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
