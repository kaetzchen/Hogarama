apiVersion: v1
kind: Service
metadata:
  name: {{ include "hogarama-commons.fullname" . }}
  labels:
    {{- include "hogarama-commons.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hogarama-commons.selectorLabels" . | nindent 4 }}
